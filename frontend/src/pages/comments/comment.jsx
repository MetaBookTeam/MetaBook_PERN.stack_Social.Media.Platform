/* import React from 'react';

const Comment = ({ singleComment, likeComment, dislikeComment }) => {
  return (
    <div>
      <p>{singleComment.text}</p>
      <p>Likes: {singleComment.likes}</p>
      <p>Dislikes: {singleComment.dislikes}</p>
      <button onClick={() => likeComment(singleComment.postId, singleComment.id)}>Like</button>
      <button onClick={() => dislikeComment(singleComment.postId, singleComment.id)}>Dislike</button>
    </div>
  );
};

export default Comment; */
